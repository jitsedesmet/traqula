{
  "name": "TRAQULA",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "description": "",
  "main": "src/index.ts",
  "files": [
    "out",
    "lib/**/*.d.ts",
    "lib/**/*.js",
    "lib/**/*.js.map"
  ],
  "scripts": {
    "build": "tsc && node esbuild.mjs",
    "clean": "git clean -dfx",
    "lint": "eslint . --cache",
    "lint-fix": "eslint . --fix",
    "build & run ESM": "yarn build && node src/index.js",
    "build & run CJS": "yarn build && node out/index.cjs",
    "test": "yarn build && vitest",
    "bench": "yarn build && node bench/benchmark.js"
  },
  "dependencies": {
    "chevrotain": "^11.0.3",
    "rdf-data-factory": "^1.1.2"
  },
  "devDependencies": {
    "@rdfjs/types": "^1.1.2",
    "@rubensworks/eslint-config": "^3.0.0",
    "@types/sparqljs": "^3.1.12",
    "esbuild": "^0.24.0",
    "eslint": "^8.57.0",
    "sparqljs": "^3.7.3",
    "tinybench": "^3.0.7",
    "ts-jest": "^29.2.5",
    "typescript": "^5.3.3",
    "vitest": "^2.1.8"
  }
}
