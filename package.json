{
  "name": "TRAQULA",
  "packageManager": "yarn@1.22.22",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && node esbuild.mjs",
    "clean": "git clean -dfx",
    "lint": "eslint . --cache",
    "lint-fix": "eslint . --fix",
    "build & run": "yarn build && node out/index.cjs"
  },
  "private": true,
  "dependencies": {
    "chevrotain": "^11.0.3"
  },
  "devDependencies": {
    "@rubensworks/eslint-config": "^3.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^8.57.0",
    "typescript": "^5.3.3"
  }
}
